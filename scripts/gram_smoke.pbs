#!/bin/bash
#PBS -q debug-g
#PBS -l select=1
#PBS -l walltime=00:15:00
#PBS -W group_list=gj26
#PBS -N gram_smoke
#PBS -j oe
#PBS -o /work/gj26/b20090/GRAM_Reproduce/wandb_stdout/

# Smoke test of GRAM model on Sudoku-Extreme: builds the new model, runs ~1 epoch
# at small batch, and exits. Just to verify the model + ELBO loss head are wired up.

set -eu
GRAM=/work/gj26/b20090/GRAM_Reproduce
AKORN_VENV=/work/gj26/b20090/fast-slow-learning/env_akorn

module purge
module load python/3.10.16
module load cuda/12.6
module load cudnn/9.5.1.17

# shellcheck disable=SC1091
source "$AKORN_VENV/bin/activate"
export PYTHONPATH="$GRAM/env_extra${PYTHONPATH:+:$PYTHONPATH}"

cd "$GRAM/external/TRM"

export WANDB_MODE=offline
export WANDB_DIR="$GRAM/wandb"
mkdir -p "$WANDB_DIR"

# Use the GRAM arch (with mlp_t=True for Sudoku per paper Section D.5);
# 1 short epoch at small batch to verify forward/backward.
python pretrain.py \
  arch=gram \
  data_paths="[$GRAM/data/sudoku-extreme-1k-aug-1000]" \
  evaluators="[]" \
  epochs=100 eval_interval=100 \
  global_batch_size=64 \
  lr=1e-4 puzzle_emb_lr=1e-4 \
  weight_decay=1.0 puzzle_emb_weight_decay=1.0 \
  arch.mlp_t=True arch.pos_encodings=none \
  arch.L_layers=2 \
  arch.H_cycles=3 arch.L_cycles=6 \
  +run_name=gram_sudoku_smoke ema=True
